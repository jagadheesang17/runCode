╔══════════════════════════════════════════════════════════════════════════╗
║                    OAUTH TOKEN MANAGEMENT - QUICK REFERENCE              ║
╚══════════════════════════════════════════════════════════════════════════╝

📁 TOKEN CACHE LOCATION
   └─ data/apiData/tokenCache.json

⏱️  TOKEN LIFECYCLE
   └─ Expires: 60 minutes (3600 seconds)
   └─ Refreshes: 45 minutes before expiry (15-minute window)
   └─ Auto-refresh: Happens on next API call after 45-min mark

💻 USAGE IN TESTS (NO CHANGES NEEDED!)
   ┌─────────────────────────────────────────────────────────────┐
   │ import { generateOauthToken } from '../../accessToken';     │
   │                                                              │
   │ const token = await generateOauthToken();                   │
   │ // Works exactly as before, now with caching!               │
   └─────────────────────────────────────────────────────────────┘

🔄 NEW API (RECOMMENDED FOR NEW CODE)
   ┌─────────────────────────────────────────────────────────────┐
   │ import { getOauthToken } from '../utils/tokenManager';      │
   │                                                              │
   │ const token = await getOauthToken();                        │
   │ // Cleaner API, same benefits                               │
   └─────────────────────────────────────────────────────────────┘

🔧 USEFUL COMMANDS
   ┌─────────────────────────────────────────────────────────────┐
   │ # Test the system                                            │
   │ npx ts-node test_token_management.ts                        │
   │                                                              │
   │ # Check token status                                         │
   │ npx ts-node check_token_status.ts                           │
   │                                                              │
   │ # Force new token (PowerShell)                               │
   │ Remove-Item "data/apiData/tokenCache.json"                  │
   └─────────────────────────────────────────────────────────────┘

📊 CONSOLE OUTPUT EXAMPLES
   ┌─────────────────────────────────────────────────────────────┐
   │ Using cached token                                           │
   │ Token expires in 52 minutes                                  │
   └─────────────────────────────────────────────────────────────┘
   ┌─────────────────────────────────────────────────────────────┐
   │ Token expired or invalid, generating new token...            │
   │ New token generated. Expires in 3600 seconds (60 minutes)   │
   │ Token cached at 2025-12-15T10:30:00.000Z                    │
   └─────────────────────────────────────────────────────────────┘

🐛 TROUBLESHOOTING
   ┌─────────────────────────────────────────────────────────────┐
   │ Issue: Token keeps regenerating                              │
   │ Fix: Check system time is correct                            │
   │                                                              │
   │ Issue: Token file not updating                               │
   │ Fix: Verify write permissions on tokenCache.json            │
   │                                                              │
   │ Issue: Need to force refresh                                 │
   │ Fix: Delete tokenCache.json or use refreshOauthToken()      │
   └─────────────────────────────────────────────────────────────┘

📚 DOCUMENTATION
   ├─ IMPLEMENTATION_SUMMARY.md ............. Complete overview
   ├─ TOKEN_MANAGEMENT_README.md ............ Technical details
   └─ api/apiTestIntegration/TOKEN_USAGE_GUIDE.md .. Quick guide

⚙️  CONFIGURATION
   └─ Edit utils/tokenManager.ts
      const TOKEN_VALIDITY_MINUTES = 45; // Change refresh timing

✅ BENEFITS
   ✓ 95% fewer token generation calls
   ✓ Faster test execution (instant vs 3-5 seconds)
   ✓ No expired token errors
   ✓ Automatic management
   ✓ Easy debugging with timestamps
   ✓ Works with all existing tests

═══════════════════════════════════════════════════════════════════════════
                      Ready to use! No code changes needed.
═══════════════════════════════════════════════════════════════════════════
